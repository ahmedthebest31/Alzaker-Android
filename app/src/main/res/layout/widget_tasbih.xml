<?xml version="1.0" encoding="utf-8"?>
<!-- Tasbih widget, 100% stock views only: OEM launchers refuse to inflate
     custom view classes or fonts inside RemoteViews ("can't display widget").
     Layout: big count, thin horizontal progress bar, and a bottom row with the
     decorative "N/M" label (hidden from the screen reader) plus the reset
     button. All colors are @color/... version-qualified (values-v31 ->
     Material You, base -> brand fallback). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_tasbih_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_tasbih_background"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_tasbih_count"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:contentDescription=""
        android:gravity="center"
        android:textColor="@color/widget_count"
        android:textSize="40sp"
        android:textStyle="bold" />

    <ProgressBar
        android:id="@+id/widget_tasbih_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginTop="4dp"
        android:minHeight="4dp"
        android:maxHeight="4dp"
        android:progressBackgroundTint="@color/widget_progress_track"
        android:progressTint="@color/widget_progress_fill" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_tasbih_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:importantForAccessibility="no"
            android:text=""
            android:textColor="@color/widget_nm_label"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/widget_tasbih_reset"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/widget_tasbih_reset_a11y"
            android:padding="6dp"
            android:text="@string/widget_tasbih_reset"
            android:textColor="@color/widget_reset_text"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>
